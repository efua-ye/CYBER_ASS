@model StaffPortal.Models.EditRoleViewModel
@{
    ViewData["Title"] = "Edit Role";


}
<div class="row">
    <!-- column -->
    <div class="col-sm-12">
        <div class="white-box">
            <h3> Edit Roles</h3>
            <div class="table-responsive">


                <form asp-action="EditRole" method="post" class="mt-4">
                    <div asp-validation-summary="All" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="Id"></label>
                        <input asp-for="Id" disabled class="form-control" />
                    </div>
                    <div class="form-group">
                        <label asp-for="RoleName"></label>
                        <input asp-for="RoleName" class="form-control" />
                        <span asp-validation-for="RoleName" class="text-danger"></span>
                    </div>
                    <div>
                        <button type="submit" class="login100-form-btn">Update</button>

                        <a asp-action="ListRole" class="login100-form-btn"> Cancel   </a>

                    </div>
                </form>
            </div>
        </div>
    </div>
</div>


<div class="row">
    <!-- column -->
    <div class="col-sm-12">
        <div class="white-box">
            <h2> Users in this Roles:</h2>
            <div class="table-responsive">
                <form>

                   
                    <div class="card-body">
                        @if (Model.Users.Any())
                        {
                            foreach (var user in Model.Users)
                            {
                                <h3 class="card-title">@user</h3>
                            }
                        }
                        else
                        {
                            <h3 class="card-title">None at the moment </h3>
                        }
                    </div>
                    @*<button type="submit" class="login100-form-btn">Add or Remove User</button>*@
                    <a asp-action="EditUsersInRole" asp-controller="Administration" asp-route-roleId="@Model.Id"
                       class="btn float-right hidden-sm-down btn-success">
                        Add or Remove User
                    </a>

                </form>
            </div>
        </div>
    </div>
</div>
