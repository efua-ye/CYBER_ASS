@model IEnumerable<StaffPortal.Entities.Grade>
@{
    ViewData["Title"] = "Grades";
}
<!-- ============================================================== -->
<div class="row bg-title">
    <div class="col-lg-12">
        <h4 class="page-title">Grade</h4>
        <ol class="breadcrumb">
            <li><a href="#">Dashboard</a></li>
            <li class="active">Grade</li>
        </ol>
    </div>
    <!-- /.col-lg-12 -->
</div>

<div class="row">
    <div class="col-sm-12">

        <div class="white-box">

            <a asp-controller="Grade" asp-action="Create" class="btn float-right hidden-sm-down btn-success"><i class="mdi mdi-plus-circle"></i>New Grade</a>


            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Level</th>
                            <th>Step</th>
                            <th>Created By</th>
                            <th>Date Created</th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var grade in Model)
                        {
                            <tr>
                                <td>@grade.Id</td>

                                <td>@grade.GradeName  </td>
                                <td>@grade.Level </td>
                                <td>@grade.Step </td>
                                <td>@grade.CreatedBy  </td>
                                <td>@grade.DateCreated    </td>

                                <td><a asp-action="Edit" asp-route-id="@grade.Id" alt="Edit"><i class="ti-pencil" aria-hidden="true"></i></a> </td>
                                <td><a onclick="return confirm('Are you sure you want to delete this record');" asp-action="Delete" asp-route-id="@grade.Id"><i class="ti-trash" aria-hidden="true"></i></a> </td>

                            </tr>
                        }
                    </tbody>
                </table>

            </div>
        </div>
</div>
        </div>
   
